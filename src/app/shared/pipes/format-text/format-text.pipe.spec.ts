import {FormatTextPipe} from './format-text-pipe';

describe('SpaceAfterComaPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTextPipe();
    expect(pipe).toBeTruthy();
  });
});
