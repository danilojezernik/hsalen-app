import { SlovenainDatePipe } from './slovenain-date.pipe';

describe('SlovenainDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SlovenainDatePipe();
    expect(pipe).toBeTruthy();
  });
});
