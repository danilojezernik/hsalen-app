import { FirstLetterCapitaldPipe } from './first-letter-capitald.pipe';

describe('FirstLetterCapitaldPipe', () => {
  it('create an instance', () => {
    const pipe = new FirstLetterCapitaldPipe();
    expect(pipe).toBeTruthy();
  });
});
