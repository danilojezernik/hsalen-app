import { SlovenianDateTimePipe } from './slovenian-date-time.pipe';

describe('SlovenianDateTimePipe', () => {
  it('create an instance', () => {
    const pipe = new SlovenianDateTimePipe();
    expect(pipe).toBeTruthy();
  });
});
