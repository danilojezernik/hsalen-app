import { HideHeaderFooterDirective } from './hide-header-footer.directive';

describe('HideHeaderFooterDirective', () => {
  it('should create an instance', () => {
    const directive = new HideHeaderFooterDirective();
    expect(directive).toBeTruthy();
  });
});
